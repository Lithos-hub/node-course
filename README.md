# Notas:

```
Options:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -t, --tabla    Toma la base de la tabla de multiplicar    [número] [requerido]
  -l, --list     Muestra la tabla por consola                         [booleano]

  -h, --hasta    Establece el límite de la tabla                        [número]
```
